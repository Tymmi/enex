'use strict';

describe('myApp.api_old module', function() {

  beforeEach(module('myApp.api_old'));

  describe('api_old controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});